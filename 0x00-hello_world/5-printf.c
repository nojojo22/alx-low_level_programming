#include <stdio.h>

/**
 * main - print a c program using printf,
 * but the outcome is a piece of art,
 * Return: 0 if exited properly, noon-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

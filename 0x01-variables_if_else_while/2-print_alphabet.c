#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 * Discription: prints the alphabet in lowercase
 * Return: Always 0 (sucess)
 */
int main(void)
{
	char ch = 'a';

	printf("Lowercase English Alphabet:\n", n);
	while (ch <= 'z')
	{
		printf("%c", ch);
		ch++;
	}
	printf("\n");

	return (0);
}

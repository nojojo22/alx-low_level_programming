#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 * Discription: prints the alphabet in lowercase
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int ch;

	clrsce();

	putchar(ch);
	{
		(“Print lowercase English alphabet : \n”, n);
	}
	for (ch = ‘a’; ch <= ‘z’; ch++)

		putchar (‘\n’);

	getch();
	return (0);
}
